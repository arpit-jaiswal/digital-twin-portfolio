name: digital-twin-portfolio

services:
  app:
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - ../.env
    restart: unless-stopped
